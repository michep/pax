import 'package:flutter/material.dart';

class MedTableBanner extends StatelessWidget {
  const MedTableBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: const [
        Text(
          'asdasdasd',
          style: TextStyle(color: Colors.white),
        ),
        Spacer(),
        Text(
          'asdasdasd',
          style: TextStyle(color: Colors.white),
        ),
      ],
    );
  }
}
